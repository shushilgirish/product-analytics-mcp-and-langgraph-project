# For the S3Hook
apache-airflow-providers-amazon

# Memory usage, environment loading, HTTP calls
psutil
python-dotenv
requests

# For local PDF fallback counting
PyMuPDF    # fitz
PyPDF2
pdf2image  # requires poppler at the system level

# For your Mistral calls
mistralai>=1.5.1,<2.0.0

# Pinecone vector storage
pinecone>=2.0.0

# For embeddings
sentence-transformers==2.2.2
huggingface_hub==0.11.1
torch>=2.0.0

# For chunking computations
numpy

# Add this for OpenAI embeddings
openai>=1.0.0
